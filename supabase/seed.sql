-- 이 저장소는 마스터 데이터(지방/도/생활권/시/포켓몬/스폰 풀)를
-- supabase/migrations/20260718100000_seed_master_data.sql에서 시딩한다 —
-- `supabase db reset`/`db seed`가 이 파일보다 먼저 그 마이그레이션을 적용하므로
-- 여기엔 추가할 게 없다.
--
-- 병합 메모: main 브랜치에 같은 경로의 pg_dump 방식 seed.sql이 있으나, 이미 위
-- 마이그레이션이 같은 마스터 데이터를 ON CONFLICT DO NOTHING으로 시딩하는 쪽이
-- 저장소 컨벤션(버전관리되는 마이그레이션)과 일치한다. 병합 시 "ours" 유지로
-- 해결해도 데이터 손실이 없다.
